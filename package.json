{
  "name": "moyu-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:client": "pnpm run dev --filter @moyu/client",
    "build:client": "pnpm run build --filter @moyu/client",
    "dev:server": "pnpm run start:dev --filter @moyu/server",
    "build:server": "pnpm run build --filter @moyu/server",
    "preview": "pnpm run serve --filter @moyu/server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dayjs": "^1.10.7",
    "typescript": "^4.3.2"
  }
}
