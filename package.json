{
  "name": "moyu-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client:dev": "pnpm run dev --filter @moyu/client",
    "client:build": "pnpm run build --filter @moyu/client",
    "server:dev": "pnpm run start:dev --filter @moyu/server",
    "server:build": "pnpm run build --filter @moyu/server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dayjs": "^1.10.7",
    "typescript": "^4.3.2"
  }
}
